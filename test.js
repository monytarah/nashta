// const node_xj = require("xls-to-json");
// node_xj(
//     {
//         input: "./data-mahasiswa.xls", // input xls
//         sheet: "data-mahasiswa", // specific sheetname
//         rowsToSkip: 0, // number of rows to skip at the top of the sheet; defaults to 0
//         allowEmptyKey: false, // avoids empty keys in the output, example: {"": "something"}; default: true
//     },
//     function (err, result) {
//         if (err) {
//             console.error(err);
//         } else {
//             console.log(result);
//         }
//     }
// );

// const fs = require('fs')

// const data = fs.readFileSync('./data-mahasiswa.xls', { encoding: 'utf-8'})
